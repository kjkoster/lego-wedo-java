#!/bin/sh
#
# A command line utility to interface with Vengit SBrick and SBrick Plus.
#

if [ -e /dev/cu.usbmodem1 ]; then
	ARGS="${ARGS} -ble112 /dev/cu.usbmodem1"
fi

java -cp target/wedo-0.0.1-SNAPSHOT.jar org.kjkoster.wedo.SBrick ${ARGS} ${*}
